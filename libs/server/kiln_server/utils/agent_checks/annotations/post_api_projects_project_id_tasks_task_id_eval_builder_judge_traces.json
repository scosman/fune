{
  "method": "post",
  "path": "/api/projects/{project_id}/tasks/{task_id}/eval_builder/judge_traces",
  "agent_policy": {
    "permission": "allow",
    "requires_approval": true,
    "approval_description": "Re-judge saved eval-builder results with a judge prompt? Invokes the judge model per result (cost)."
  }
}
